services:
  - type: web
    name: triage-mvp
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DEMO_MODE
        value: "1"
      - key: NODE_VERSION
        value: "22"
